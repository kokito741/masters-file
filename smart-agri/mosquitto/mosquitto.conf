# Mosquitto 2.0 — smart-agri config
# IMPORTANT: Mosquitto 2.0+ requires an explicit listener and allow_anonymous.
# Without this file the broker refuses all connections.

listener 1883
allow_anonymous true

listener 9001
protocol websockets
allow_anonymous true
